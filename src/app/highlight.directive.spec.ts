import { HiighlightDirective } from './highlight.directive';

describe('HiighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new HiighlightDirective();
    expect(directive).toBeTruthy();
  });
});
